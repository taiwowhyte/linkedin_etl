# These values configure S3/Athena locations CSV ingestion paths.

# AWS / Athena
LINKEDIN_ETL_BUCKET=your-bucket-name
AWS_REGION=eu-north-1
ATHENA_WORKGROUP=primary
ATHENA_RESULTS_S3=s3://your-bucket-name/athena_results/
ATHENA_DB=your_database_name

# S3 prefixes
RAW_ROOT_PREFIX=linkedin_etl/raw
CLEAN_ROOT_PREFIX=linkedin_etl/cleaned_normalised_no_dedup
CURATED_ROOT_PREFIX=linkedin_etl/curated

# Local ingestion path (where the Kaggle CSVs live)
LINKEDIN_DATA_DIR=/opt/airflow/data/1-3m-linkedin-jobs-and-skills-2024/versions/2
